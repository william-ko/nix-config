return {
  "folke/snacks.nvim",
  opts = {
    dashboard = { enabled = false },
    image = { enabled = false },
    indent = { enabled = false }, -- Disable to prevent window id errors
    scope = { enabled = false }, -- Disable to prevent scope-related errors
  },
}
